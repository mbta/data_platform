name: ExCubicOdsIngestion Docker build

on: [push]

jobs:
  docker:
    name: Build Docker image
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: docker build .
      working-directory: ex_cubic_ods_ingestion
